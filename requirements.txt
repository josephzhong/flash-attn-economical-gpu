# Core runtime for flash_atten.py
pillow==12.2.0
numpy==2.4.4
matplotlib==3.10.9
# PyTorch pinned to 2.9.x patch with CUDA 12.8 GPU wheel, which contains triton 3.5.1
# If you are on macOS/CPU-only, replace with: torch==2.9.1
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.9.1+cu128

# Test runner for test_flash_atten.py
pytest==9.0.2
psutil

# Hugging Face runtime for ttft.py. Transformers 5.5.x includes Qwen3.5 support
# and resolves cleanly with the pinned PyTorch 2.9.1 + CUDA 12.8 stack above.
transformers==5.5.4
accelerate==1.13.0

# install latest official flash attention 2 later
